# Agent Eco

Multi-user, multi-agent personal assistant (Next.js web UI + shared Telegram bot).

```bash
pnpm install
cp .env.example .env.local   # fill in
pnpm db:migrate
pnpm dev
```

See [CLAUDE.md](./CLAUDE.md) for commands, architecture, contracts, and rules.
